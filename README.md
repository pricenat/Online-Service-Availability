# Online-Service-Availability
HTTP Server Microservice for testing server availability. It expects JSON data with a "url" parameter containing the server address combined with a colon, i.e. "http://127.0.0.1:6003".
It returns the status code of the server being connected to.
